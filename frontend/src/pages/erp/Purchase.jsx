import Vendors from '../Vendors';

/** Purchase / vendor sourcing — mapped from legacy Vendors. */
export default function Purchase(props) {
  return <Vendors {...props} pageTitle="Purchase · Vendors" />;
}
